---
title: "Rollup 3 + rollup-jest → Rollup 4 + @swc/jest"
emoji: "🛼"
type: "tech"
topics:
  - "jest"
  - "rollup"
published: true
published_at: "2025-02-23 15:28"
---

Rollup 3 から Rollup 4 にしたい。けれど rollup-jest の最新 3.1.0 でも Rollup 4 に未対応

Rollup 4 にするために @swc/jest に移行

## @swc/jest インストール

package.json にある rollup-jest の行を削除後、@swc/jest をインストール

```shell
rm package-lock.json
rm -rf node_modules
npm install --save-dev @swc/jest
```

## jest.config.json → jest.config.js

rollup-jest の設定を @swc/jest に変更

### jest.config.json を削除

```json
{
  "preset": "rollup-jest"
}
```

### jest.config.js を新規作成

```js
export default {
  transform: {
    "^.+\\.js$": "@swc/jest"
  },
  verbose: true,
};
```

以上で jest test/* が動作。rollup-jest、今までありがとう